# 《见山》× JanVim：v1 双端并行实施通知

2026-09-05。用户已明确批准双方同步推进独立候选编码及自动验证。

JanVim 端已从真实光标候选 `93ce7b6f9667c1f32e4faada1d5f45130cbe224c` 建立：

- 工作树：`D:/github/JanVim-Exhibition-2026/.worktrees/sound-flock-ingress-v1`
- 分支：`feat/sound-flock-ingress-v1`
- 书面设计/计划提交：`a2d9b87`（仅计划，不是入口交付提交）

我方正在实现严格接收/新鲜度策略、角色隔离监听接线、唯一 sender 和独立风声失效。贵方可独立实施生产端、抽样、CPU/GPU 接线及测试，不必等待我方完成。

生产端继续按已冻结 v1 TCP/NDJSON 协议实现，抽样算法/时间锚点/ACK/字段/长度/epoch/一小时上限均未修改；不新增生产端接口版本。新增的 SC 内部风声截止/关闭只由 JanVim 端实现，不要求贵方直连 SC 或调整已冻结生产端。

三个共同新鲜度用例将提供实际接收器、sender 及 SC 行为证据：490ms老包只剩10ms；排队至500ms不转发；无新包时已消费目标依原期限淡出。测试替身、此前光标成果不充当真实鸟群入口交付证明。

接口就绪时我方再提供候选提交、启动命令、当次私有 flock-input.json 的取得方式和测试证据。请勿把 token 放入共享回函；双方就绪后再安排人工联合试听。

授权仅限独立候选编码与自动验证。Listen/真实音箱测试、HP部署、推送、PR、合并及替换保底另行确认。双方保底、原OSC独立试听和既有光标候选均不动。
