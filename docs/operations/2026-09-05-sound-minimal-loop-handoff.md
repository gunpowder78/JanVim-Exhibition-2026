# 声音最小闭环：隔离候选交接

日期：2026-09-05。最新状态：N1 已修正并通过 52/52 声音回归，独立复核无阻塞问题；
用户确认录音与实时短演示声音正常且一致，混合段提前 Stop 人工试听也已通过。本候选仍隔离保留。

## 混合段提前停止：人工确认通过

2026-09-05，用户在 `sound-stop-n1-20260905-140741-listen` 中确认：
停止时两层共同平滑淡出，之后不再响起。核对磁盘回执：`mode:listen`、
`clean:true`、`reason:requested`，监管总时长 78.8639349 秒，15 次资源采样，
最多 7 个拨弦声部，事件及资源日志均未截断。

日志最后一个 cue 在模拟输入第 66.265 秒，同时包含 cursor 与 flock；随后 control
发起停止，确实已进入混合段。服务停止前后接受计数保持 145/146，livePlucks 由 5
降为 0，最后为 `SOUND_COMPLETE clean:true`。另核对控制端点 inactive、本次已记录
的子进程均已消失、57140/57141 无占用，声音运行源码与 `541ddc6` 的已验证字节一致。
主观平滑淡出以用户听感为依据，日志不被当作声学测量。

本次验收范围：模拟光标拨弦＋模拟鸟群风声＋统一停止淡出的最小闭环；不包含真实
JanVim 光标/《见山》鸟群输入、展厅音响系统、现场声压或异常退出的人工声学验收。
首次提前停止的 34.651 秒运行保留为历史，不冒充混合段试听。

独立记录：`D:/VirtualData/JanVim-Exhibition-Rehearsals/sound-n1-followup-20260905-1348/human-audition-receipt.json`。
本次只更新验收文档，未修改声音/展演代码，也未自动播放、合并或推送。

## N1 授权续作与基本试听确认

用户在报告两次声音一致后授权“请继续推进”。本次只删除 `owned-process.cs` 淡出条件中的
退出码限制：READY 后缺少 COMPLETE 时，退出码 0 和非零均保留既有 3600 毫秒宽限；
正常 COMPLETE 不额外等待。没有改合成图、音色、OSC、驱动、展演代码或 artifact。

先在旧代码上新增非音频进程回归，实际因后代提前消失（`ESRCH`）失败，再做一行修正。
完整声音集 52/52 通过，222.351 秒，零失败/跳过。三种退出路径实测总收尾时间分别为
3728.150 毫秒（0/无 COMPLETE）、3723.114 毫秒（非零/无 COMPLETE）、101.272 毫秒
（0/有 COMPLETE），并验证了后代回收与端口复用。这里是请求至宿主关闭的总时间，
不是声学淡出时长测量；真实信号淡出另由现有录制帧正反对照和 NRT 检查覆盖。

独立复核确认 N1 已解决，无 Critical/Important；保留两项非阻塞测试说明：
3.1 秒存活探测受系统调度影响，重负载下可能误失败；总收尾时间包含请求与宿主开销。
未为此增加计时协议或重构进程宿主。以前的 30 分钟耐久证据仍只属于 `0cb5a9c`，
没有冒充为本次一行修正后的耐久重测。原有应用代码、Electron bundle 和展演保底未变。

用户基本试听记录（均已核对对应 summary，`clean:true` / `reason:requested`）：

- 录音：`sound-20260905T045509603Z-36b30bfd02ef`，默认 silent 内部录音，之后由用户播放 WAV。
- 实时：`sound-20260905T045612903Z-a6f1ce942d01`，用户主动 `-Listen`。
- 用户结论：“.wav 和终端演示的声音正常且一致”。记录为基本音色与一致性确认，
  不擅自扩展为提前停止、异常声学淡出或展览联动验收通过。
- 之后的 ENOENT 来自误执行 `sound-本次运行目录` 占位路径，不是服务故障。
  两次实际运行均已结束，不需要再停止。README 已改为明确输入真实目录；本次人工窗口
  另提供预填目录的文件脚本，不依赖会话变量，silent/listen 使用两个互不复用的目录。

续作证据与 A/B 脚本目录：
`D:/VirtualData/JanVim-Exhibition-Rehearsals/sound-n1-followup-20260905-1348/`。
三项 SC 策略/隔离检查、lint 和 diff 检查均退出 0；预填目录的 A/B 文件脚本已从
`C:/Windows/System32` 实际静音启动并提前停止，两个命令退出 0，32.429 秒后
`clean:true` / `reason:requested`，控制端点关闭。本次没有自动开启硬件声音。
当时为人工 Listen 准备了独立的新目录；后续实际混合段试听结果见本文件顶部。
下文 N1 未结叙述属于此前夜航历史，由本节的授权续作结果更新；旧回执不覆盖删除。

## 此前夜航审查历史

整分支审查补充：发现启动早期的孤儿音频进程清理缺口，以及实时录音尾部零值取自
未录制缓冲区的问题，已在 `0cb5a9c` 集中修正，定向复核确认两项均解决。下文旧版本测试通过和数值记录保留为历史；
旧实时 WAV 尾部的零值不能作为“已验证实际停止淡出”的证据。独立 NRT 淡出与
停止后接受计数检查仍各自有效，但不能替代实时录音证明；修正后的实录证明见后文。
由于 N1 尚未解决，当前不可宣告全部自动验收完成。

定向复核新增 N1（Important，代码检查确认）：`sound/owned-process.cs:121` 只对非零退出码
给予 DSP 租约/淡出宽限。如果 READY 后语言进程退出码为 0、却没有 `SOUND_COMPLETE`，
Job 会立即关闭并硬切声音。正常停止和已测故障通过不覆盖此分支。
由于缺少 COMPLETE，监管回执仍会标记 `clean:false` / `languageExit`，不会将它记为正常完成。
本轮审查修正上限已到，保留明确未结项而不继续扩张修正循环；未豁免原设计的异常淡出要求。
下一次只需针对此条件补失败测试、修正并定向验证；在此之前不合并、不推送、不宣称全部自动验收通过。

最小后续补丁范围：先在 `sound/tests/startup-lifetime.check.mjs` 添加“READY 后、
没有 COMPLETE、退出码 0”的非音频子进程回归；应证实后代在宽限期内仍存活、之后被回收、
端口可复用，再将宽限条件改为只依据 READY/COMPLETE，而不是退出码。
同时保留正常 COMPLETE 路径与总清理时限的覆盖。无需改音色、OSC、驱动或展演业务代码。

## 保底与范围

本候选位于 `.worktrees/sound-minimal-loop` / `feat/sound-minimal-loop`，起点为
`abd471c1d05d779dd6df5f00bc1511ac9ce1d41d`。现有可展览视觉版本不依赖本模块，
未被替换；本次不合并、不推送、不更新其标签或 artifact lock。

只实现模拟光标拨弦、模拟鸟群风声和统一停止淡出。夜间测试不向硬件输出声音。
模拟输入通过实际 OSC 和 SuperCollider 合成链路，但不代表已接入真实 JanVim
光标或《见山》鸟群。合成信号自动检查不替代人工试听或现场验收。

## 已知环境

- SuperCollider 3.14.1；Node 22.23.0；PowerShell 7.6.5。
- 语言端口 `127.0.0.1:57140`，音频服务端口 `127.0.0.1:57141`；每次启动检查占用，
  不承诺永久空闲，也不终止已有占用者。
- 试听目标仅为 `Windows WASAPI : Headphones (Senary Audio)`，48 kHz、双声道、零输入。
  不修改驱动、系统音量或网络设置，不自动切换到扬声器。
- 用户已确认官方 440 Hz 示例可听见；不再把旧低增益测试的无声当作驱动故障。

## 验证记录

隔离工作树先完成原有基线：55 个测试文件、982 项测试通过；
`npm ci`、typecheck、build、lint、Lua 测试和 runtime 校验均通过。
首次运行缺少被 Git 忽略的冻结 runtime 和构建产物；补齐经哈希验证的副本后完整重跑通过，
没有修改展演业务代码来绕过测试。

基线 Electron bundle 为 526622 字节，SHA-256：
`acbe557eb15c3ffa32f936ed8e74a2a6e0d3284508a95f56c6ef71e856eb9bd0`。
声音模块最终测试与哈希复核见本节后部；早期记录仅用于保留验证历史。

2026-09-05 07:15 的应用复核再次通过：55 个文件、982 项测试，耗时 688.47 秒；
npm ci、typecheck、build、lint、Lua/runtime 校验全部退出 0。
artifact lock、fixture manifest、TOML 和 Electron bundle 的字节数及哈希均与基线一致。
此后声音模块的新增测试及最终 lint/scope 复核仍需完成，不把应用测试当作声音验收。

09:01:58 启动的收尾应用测试再次全部通过：55 文件、982 项，686.65 秒。
随后 typecheck、build、Lua/runtime 均退出 0，Electron bundle 哈希保持一致。
对应独立回执为 `closing-application-tests.json` 和 `closing-build-runtime-gates.json`。

消息层及隔离启动已完成定向测试和独立审查（`0438c67`、`5bb152a`）：
OSC 字节编码、会话/顺序/时效、限频、心跳停止锁定、计数饱和、10 小时假时钟测试，
以及用户启动钩子隔离、端口占用拒绝、日志截断和有界退出。10 小时是假时钟模拟，
不是连续运行了 10 小时。启动隔离仅通过本项目子进程配置生效，没有修改用户的 SC 配置。

合成服务已通过 14 项 WAV/服务检查及独立复核（`4e95208`、`2f394e9`、`ce3e2ae`）：
实际 NRT 信号、停止后静音、独立 DSP 心跳淡出、采样数限制、端口占用拒绝、
启动取消、录音写盘失败回执及有界清理。故障清理实测 6995 毫秒；无事件实时对照录音
两声道均为零。

首轮实际 OSC 静默长测于 08:45 左右结束，证据目录为
`D:/VirtualData/JanVim-Exhibition-Rehearsals/sound-soak-30m-9739605a-d0ce-4dac-89db-2a2502ae163a`。
服务 start 至 stop 为 1800.027 秒；含启动和清理的监管时长为 1811.829 秒。
361 次采样，最大同时拨弦声部 7，退出 `clean:true`，事件和资源日志均未截断。
峰值工作集分别为监管进程 60,923,904、sclang 142,741,504、发送器 56,016,896、
scsynth 35,852,288 字节。该短期观测不能证明无限期无内存增长。

这一轮使用 lint 修正前的脚本，源码身份单独保存在运行目录及总证据目录的
`soak-running-source-audit.json`；不将它冒充为后续代码版本的长测。
实际子进程中断测试已生成初步回执，仍需 Task 3 独立审查和最终版本验证。

`3b050ba` 版本完整声音复核已通过：34/34 Node 检查（146.456 秒）及 3 个
PowerShell/SC 策略与隔离检查全部退出 0；最终 lint 退出 0。原有应用之外的差异
仅在 `sound/` 和文档中。完整输出保存在总证据目录的 `sound-gates-3b050ba.json`。

实际 UDP 集成录音位于
`D:/VirtualData/JanVim-Exhibition-Rehearsals/sound-integration-15dc7d55-230f-4371-b817-48bb2bcef56a/capture.wav`，
SHA-256 为 `b73ee748e1cd1d7d0c34eb3869954d98de9cc4b7982b088a8445e52a40a8f954`。
拨弦、风声、混合段峰值分别为 0.2000122、0.0464783、0.1980896；
PCM16 量化允许一格误差，两声道均无满幅削波样本。旧停止检测段为零，
但该区间可能是未录制的缓冲区填零，已撤回其作为实时停止淡出验收证据的解释。
文件包含预分配缓冲区，不把文件时长等同于持续有效输入的时长。

独立 NRT 使用同一生产合成图，目录 `sound-nrt-pKOrk9`，
实际拨弦与风声非零，正常淡出后和 DSP 租约过期后均为零。
无事件实时对照目录 `sound-service-rRy0l5`，2.5 秒两声道全零。
生成声音没有经耳机播放；这些是数值信号证据，不是主观试听结论。

本次监管进程实际中断测试分别得到了预期的 `languageExit`、`senderExit` 失败回执；
前者回收其已确认身份的孤儿音频子进程，后者由 `heartbeatTimeout` 停止合成。
两种情况之后均验证端口可复用且新会话干净退出。失败注入是测试预期，不写成普通运行成功。
文件式启动/停止已在 `C:/Windows/System32`、无预载变量环境下验证。

审查修正 `dd83918` 补齐了三个边界：停止重试共用 300 毫秒截止时间；
控制回执初始/停止写盘失败不泄漏监听器或产生未处理拒绝；实际 UDP 停止后
比较接受计数而不只检查静音。修正的 11 项监管、4 项实际集成、14 项 WAV/服务回归通过，
独立复核已确认三个问题均解决、没有新增问题。实际停止前后计数为 13/14 → 13/14，后续接受增量为 0/0；
证据目录 `sound-integration-abb3e746-0332-4359-af51-6a3a6955d3ad`。

主代理随后重新执行完整声音集：37/37，147.332 秒；三项 SC 检查及全项目 lint 均退出 0。
完整输出为 `sound-gates-dd83918.json`。等待整分支审查和最终源码版本的 30 分钟复核；
尚未进行人工试听。

### 最终安全修正与重新验证

`0cb5a9cd2b77a03e09360f59bcf455733e1672f0` 在创建语言进程时原子地加入本次私有
Windows Job；不等 READY 才取得子进程生命周期归属。实际 sclang 与新增宿主进程
分别记录身份和资源，不用端口号或磁盘 PID 作为杀进程的依据。正常停止仍先淡出，
语言进程意外消失后保留 DSP 心跳超时及淡出时间，再回收本次后代。

修正后主代理重新执行 49/49 声音测试，209.900 秒，零失败/跳过；三项 SC 检查、
全项目 lint 和差异范围检查均通过。实际故障清理为 7417 毫秒；语言中断清理为
5562.395 毫秒，并验证了新的静音会话可正常启动。回执为 `sound-gates-0cb5a9c.json`。

实时停止正反对照的确切字节与数据在 `final-source-and-recorded-stop.json`：

| 检查 | 正常停止 | 故意省略混音器停止 |
| --- | ---: | ---: |
| 停止前双声道峰值 | 0.151947 / 0.058838 | 0.189636 / 0.069092 |
| 已录制帧前缀 | 695936 | 691136 |
| 停止标记帧 | 556736 | 554368 |
| 结束后 1.6–1.7 秒的峰值 | 0 | 0.0234375 |

两个测量窗均严格落在已录制、且声部尚未释放的区间；负面对照仍存在尾部填零，
但真实录制窗不静音，所以检测按预期失败。它证明了检查不是靠缓冲区填零或提前释放声部通过。
正常版本停止后接受增量为 0/0；两声道无满幅削波。人工音色、声压与舒适度仍未验收。

### 冻结源码的最终 30 分钟静音运行

运行目录：`D:/VirtualData/JanVim-Exhibition-Rehearsals/sound-final-soak-20260905-0219-0cb5a9c`。
实际活动 1800.034 秒，含启动/清理 1813.730 秒；退出 0、`clean:true`、`reason:requested`。
361 次资源采样，零采样错误，最多 7 个拨弦声部；事件/资源日志均未截断。
峰值工作集：监管 61,300,736、sclang 142,655,488、服务宿主 172,105,728、
发送器 56,795,136、scsynth 35,811,328 字节。该有限时长观察不证明无限期无内存增长。
没有创建录音文件、没有硬件声音输出；运行前后 13 个声音运行文件字节哈希一致。
结束后确认本次六个声音进程身份对应的 PID 均已消失，57140/57141 无占用。

这轮是 `0cb5a9c` 运行源码的正常停止资源证据，不覆盖 N1 的未测异常退出分支。
回执：`D:/VirtualData/JanVim-Exhibition-Rehearsals/sound-night-20260905-0535/final-soak-audit.json`。
完整汇总为同目录 `final-implementation-receipt.json`；审查资料另存 `sdd-review-archive-0cb5a9c/`。
本工作树及本地提交保留，不合并、不推送；原可展览视觉版本及 Electron bundle 保持原哈希。

## 后续真实输入接入点（本次只读分析）

### JanVim

当前 Lua agent 的动作完成 ACK 不是实时光标轨迹；坐标中的 `byteCol0` 是 UTF-8
字节列，不是屏幕像素。不得将 ACK 或字节坐标伪装成剑客动画运动。

最低成本的后续选择是“编辑器动作节奏”：在 `nvim/lua/janvim_exhibition/actions.lua`
现有 move、insert、selection 实际应用后观察光标状态，至多 8 Hz，只保留最新采样。
对经输入队列执行的动作，应确认应用时点或使用相应光标事件，不能只在排队之后取值。
当前 `bridge-server.ts` 只接受 command/ACK，新增观测需显式协议类型及有界队列。

若要求精确跟随 JanVim 渲染后的剑客轨迹，则需要产品渲染层导出并交付新的冻结 artifact；
本候选不改产品源码、不重建 JanVim。

### 《见山》

当前 SCREEN-3 的 `show/jianshan-standby.html` 是禁用脚本和联网的安全占位画面，
没有鸟群状态。只读检查发现：

- `D:/github/JianShan`，commit `ece64ca46a18bcde4f1b5dad2f03f8f4a6ad7854`：
  Python/ModernGL 重力场程序，没有所需 OSC 鸟群导出器。
- `D:/github/JianShan02`，commit `7ba25667e61fa3bdca119cecfca729ce6b36a0da`：
  Web 和 Rust 鸟群实现，没有所需 OSC 导出器；Web 现有 density 不等同运动 energy。
  Rust GPU 路径不更新 CPU boids，不能把其旧数据称为真实 GPU 鸟群状态。

后续先确定部署仓库、版本和 CPU/GPU 路径，再导出少量聚合特征：归一化横向质心、
明确定义的平均速度能量及鸟数，至多 20 Hz；不发送逐鸟数组。
若采用已检查的 Web 版本，还需处理其现有 Tone 风声路径，避免它与 SuperCollider
同时重复出声；这属于后续真实接入，不在今晚修改范围内。

### 生命周期边界

正常诗文 reset 是非终止事件，不应映射为本原型不可恢复的 OSC `stop`。
真正停止或故障则立即停止声音；恢复后需新的声音会话并明确重新启用，不能自动恢复可听输出。
任何观测、发送或声音故障都不得阻塞视觉 cue、reset、安全占位或控制器清理。

## 音色创作备忘（未实施）

光标乐句后续可加入古雅拨弦、经典电子乐与鲸鸣；鸟群气息和声场后续可加入自然天籁、
地籁、宇宙洪荒感、电子杂讯和宇宙热噪的艺术化声音。这些是创作方向，不是当前闭环的交付内容，
也不是对真实宇宙声音采样来源的声明。

## 人工窗口

可先人工预听本次真实 UDP 合成的
[正向测试录音](D:/VirtualData/JanVim-Exhibition-Rehearsals/sound-integration-f39b46e2-d9f3-407a-92e1-9ae9aed918e4/capture.wav)。
文件前几秒是启动静音；由人先调低耳机音量，不要因开头无声而提高音量。
这是音色参考，不是实时故障淡出的验收，也不自动播放。不要把省略停止的负面对照当作试听演示。

启动/停止指令见 [声音候选使用说明](../../sound/README.md#运行与停止)，已经实际静默验证。
N1 定向修正已完成，用户已确认基本音色、录音/实时一致性及混合段提前 Stop 的共同
平滑淡出与不再响起。本次人工窗口结束，不需要重复这次测试；仍不标为声音展览联动验收。

试听分两遍即可，不需要重跑视觉或断网验收：第一遍听完整的短演示，第二遍在混合段
从另一终端发出停止指令，确认两层声音共同淡出、不再被旧消息唤起。
任何刺耳、突发过响或无法停止均判为未通过；不要用提高音量来排除无声故障。
试听结束保持服务关闭，再决定是否进入真实输入接入阶段。
