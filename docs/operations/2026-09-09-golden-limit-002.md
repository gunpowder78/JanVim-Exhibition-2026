# 展示版黄金极限 #002 固化记录

用户完成光标节奏试听后确认“效果非常好，淡出后无复响”，随后完成带真实《见山》的三屏
联调并确认“画面和整体声音都正常”，授权正式固化本版本。

本版声音实现为 `b5fbadb43cdcc70b4303b533bcb1a6ead195fea9`。发行改动仅在既有
`deployment/operator/Start-Exhibition.ps1` 的 Sound 动作增加
`-InstrumentProfile StoneAndSignalV2`；没有改变三屏启动顺序、关闭逻辑、计划任务、
显示回退规则、原诗、JanVim artifact 或《见山》运行时。

已先执行失败测试，证明旧正式入口缺少该参数；改动后实际 Sound 阶段构造的子进程参数
检查通过。此前离线打包测试把三种独立拒绝场景放在同一 5000 ms 用例内，实测累计约
5325 ms；本轮拆成三项，保留所有原断言、文件保全检查及单项时限，三项均通过。
完整门禁及发布前验证的精确结果记录在外部冻结目录，不能用定向通过替代全量结果。

本轮 `npm ci`、`npm run typecheck`、`npm run build`、`npm run lint` 均退出 0。
默认并行 `npm test` 耗时 892.58 秒：68 个文件中 66 通过、2 失败；1325 项中
1321 通过、4 超时。超时为 `g2-launcher` 的无效路径/显示 ID 拒绝用例，以及
`offline-package` 的便携来源一致性、物理 A/B 分支和非精确来源身份用例，实测
5092 / 7222 / 5409 / 7074 ms，超过原有 5000 ms 时限。没有延长时限或删掉断言；
保留默认全量失败状态及独立串行复测回执。声音实现此前的 184 项测试通过；本轮
不修改已试听的声音代码，也不把离线测试当成现场声音验收。

随后将 `deployment-operator`、`g2-launcher`、`offline-package` 三个文件按
`--maxWorkers=1` 复测，53/53 通过，耗时 87.35 秒；保留每项 5000 ms 原时限。
默认并行全量仍记为未全通过，不能将串行结果写成默认全量通过。

完整三屏会话：

```text
joint-session-20260909T061742639Z-3f9acb215431
full-three-screen-cursor-rhythm-20260909T061741163Z
```

机器报告记录 4 轮、0 重试、0 恢复、累计可见漂移 45.5134 ms；声音运行
485.5477495 秒，`clean=true / reason=requested`，最大乐器节点计数 5。
Show 退出 0，《见山》按持有的进程身份正常关闭。临时联调收尾脚本在声音已结束后再次
调用 StopSound，产生 `invalid control receipt` / exit 1 的多余请求诊断；原件保留，
未读取控制凭据。正式入口继续使用原有的终态检查及有条件停止流程，不引入这段临时脚本。

用户最后保存的混音为 Wind `+6 dB`、Instrument `-2 dB`，文件时间
2026-09-09 06:24:35 UTC。保留展厅人工映射；本轮工作室按默认扩展顺序使用：

| 画面 | 设备 | 位置与分辨率 | 缩放 |
| --- | --- | --- | --- |
| JanVim | DELL S2309W / 779793126 | 0,0 / 1920×1080 | 100% |
| Narrative | Mi TV / 3513127176 | 1920,0 / 1920×1080 | 100% |
| 《见山》 | DELL U2410 / 407237437 | 3840,-133 / 1920×1200 | 100% |

现场配置快照随包放在 `deployment/docs/golden-002-site/`，不会自动写入活动现场配置。
已有桌面入口和 `Start_JanVim_Exhibition` 登录后 `PT30S` 任务仍指向固定部署目录；
旧 `Start_JianShan_Boid` 保持禁用。

封包使用独立输出；候选排练缓存和上一份固定部署分别原样保留，不做 reset、clean、强推、
合并或覆盖安装。本地冻结标签为 `exhibition-golden-limit-002-2026-09-09`；
未授权远端发布，因此本轮只做本地提交、标记、安装和归档。

固定部署：`D:\github\JanVim-Exhibition-Deploy`。
冻结过程证据：`D:\VirtualData\JanVim-Exhibition-Rehearsals\golden-limit-002-freeze-20260909`。
最终归档：`D:\VirtualData\JanVim-Exhibition-Rehearsals\golden-baselines\exhibition-golden-limit-002-20260909`，
以及 `C:\Users\hxj\Documents\JanVim-Exhibition-Baselines\exhibition-golden-limit-002-20260909`。

本次固化按用户对工作室音画效果的批准执行。机器报告保持 `monitor-simulation / diagnostic`；
物理投影、离线及强制重启属于待验项目，不继承旧包或固定小样的验收结论。
